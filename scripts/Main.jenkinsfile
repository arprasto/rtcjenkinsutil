pipeline
{
  agent any
  stages
  {
    stage('Load Configuration')
    {
    steps
      {
      script
        {
        def workspace = env.WORKSPACE
        def CONFIG_PATH= "${workspace}/config/config.properties"
        echo "${CONFIG_PATH}"
        setEnvironment(CONFIG_PATH)
        }
      }
    }
  }
}
